# 2026-08-13 工作日志

## TCD 解析器：辅机海上油耗并入 + remark（用户决策）
- 用户确认：油种识别已 OK（MGO/HSFO/LSFO 三类，`fuelCat` + `docHSFO`/`sawHSFO`）；但指出**海上辅机油耗此前只算出、未写入数据**（被 `buildFuelTiers` 丢弃）。
- 用户选择「**并入主机值 + 加 remark**」（非独立列、非仅展示）。
- 改动 `shipping_schedule.html` `parseVesselTextClient`/`buildFuelTiers`/`displayParseResult`：
  1. `buildFuelTiers` 把 `seaAdd` 恒定并入每档对应油种列（主+辅=总海上油耗）。
  2. `result.fuelRemark`：有 seaAdd→"已并入≈X MT/天(:油种)，若 TCD 每档已含辅机请勿重复计算"；无→诚实说明各档主机-only。
  3. 防误判守卫：`bumpAdd` 对 seaAdd 同油种首次命中即停；reefer 行跳过；双栏 `MAINENGINE,AUXILIARYENGINE` 标题不触发 AUX；KR CELEBES 专用抓 `Aux. Sailing/Port` 基数。
- 验收：KOTA 132(122+10)、KR CELEBES 8.0/13.7/17.1/19.5(主 HSFO+1.4 辅)、均 0 警告；3 套回归 54/54、CSV、25/25 全绿（s2/Case2 预期回退为合并值 48/35.5/26.5）。
- **未推送**：严守"先测准再上线"。本地文件 `shipping_schedule.html` 已含新解析器，待用户真实样本验收后 commit/push。
- 备注：4350 / V1500 / CHANG FENG 三份 TCD 其文档未解析出独立"海上辅机油耗"项，remark 如实标注"未解析到"，各档保持主机-only（如用户提供含辅机的样本可再补）。
