# 2026-08-17 工作日志

## Port wait_time 更新到 Week 32
- 数据源：`P:\04 上海操作中心\01 船期管理科\船期管理\VSL Daily Movement\Port Condition\Port condition Year 2026 Week 32.xlsx`
- 累积档案，`workbook.xml` 反查 `name='Week 32'` → `worksheets/sheet31.xml`（非按顺序，铁坑确认）。
- 解析规则沿用 7-28 MIN：C 列 `>Xhrs`→X、`X>Yhrs`→下界 X、`<Xhrs`/`NIL`/`N`→0；D 列描述不解析；同港多码头取 min。
- **无船(NIL)边界处理（本次新增约定）**：
  - SAJED：RSGT `>19hrs`(19) + DPW `NIL`(本周无船) → 取**有船码头 min=19**，忽略无船码头的 NIL（无船≠等泊0）。
  - LYBEN：整港 `NIL`(本周无船) → **0**（无拥堵报告=正常）。
- 12 港变更：CNNGB 48→36、CNSHK 24→72、CNTAO 48→72、YEADE 24→96、SAJED 6→19、INMUN 72→24、MYPKN 120→16、MYPKW 48→60、LYMRA 24→12、THBKK 48→15、SADMM 0→2、LYBEN 24→0。
- JOAQA(148>240hrs) 不在 ports.csv，未新增（守"仅更新已有港"）。
- 流程：fetch+checkout 对齐(origin/main 7442a46) → csv 重写第6列(保留 BOM+CRLF) → `git diff` 仅12行/只动第6列 → `audit_data_loss.py` PASS(退出码0) → commit+push → `git show origin/main` 验证。
- 备份：/tmp/ports.csv.bak（Git Bash /tmp）。

## Vessel Best Model (cul_vessel_bestmodel.csv) 合并更新
- 数据源（新文件名）：`C:\CULINES\Claw Report\CUL Vessel Best Model Report - 2026.xlsx`（sheet `装载Best Model`，同旧 MEMORY 引用文件，仅改名）。
- 复用脚本 `archive/export_excel/merge_bestmodel.py`（dry-run 默认，`write` 参数才落盘）：base=当前 CSV，Excel 有值才覆盖 cols 2–10（Vessel Name=0/Service Lane=1/Remark=11 为 key 或 Excel 无，绝不覆盖），新 vessel/lane 追加。
- **lane 匹配踩坑与修复**：① 初版用 `ltk & lt` 纯 token 交叠太松——`CST(NB)` 因共享 `CST` 误匹配 `CST(SB)` 并改写其 Service Lane；改 `lane_match` 为「精确串相等 OR 一方 token 集⊆另一方」。② 同名同 lane 多行（SUNNY 68 SJA 有 laden+empties 两行、Excel 给 3 行）会塌缩丢失 laden；改「每 Excel 行只更新 fresh 中**首个不同 lane 的 CSV 行**」+ `consumed` 防重吃。最终 SUNNY 68 两行(532 laden / 650 empties) 均保留。
- 结果：Norminal Capacity 全 60 船 0 diff（CSV 本就与旧报告一致）；仅 3 处次级字段更新（WAN FU DA BSA 1620/1600、CUL HUMEN REX 货重16000/min20 50·542/BSA 1100·1050/reefer 80、TEN VENUS Max TEU2 1540）+ 3 新 lane（Chang Sheng JI 7 NP2/REX、CUL YANGPU CST(NB)）。CSV 原有重复行 CUL YANGPU [CCT(NB)]×2 未删（守铁律不删 CSV-only）。
- 校验：BOM+CRLF 保留、82 行均 12 列、`audit_data_loss.py` PASS(52=52 无丢失)。commit+push 待执行。
