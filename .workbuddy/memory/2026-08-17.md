# 2026-08-17 工作日志

## Port wait_time 更新到 Week 32
- 数据源：`P:\04 上海操作中心\01 船期管理科\船期管理\VSL Daily Movement\Port Condition\Port condition Year 2026 Week 32.xlsx`
- 累积档案，`workbook.xml` 反查 `name='Week 32'` → `worksheets/sheet31.xml`（非按顺序，铁坑确认）。
- 解析规则沿用 7-28 MIN：C 列 `>Xhrs`→X、`X>Yhrs`→下界 X、`<Xhrs`/`NIL`/`N`→0；D 列描述不解析；同港多码头取 min。
- **无船(NIL)边界处理（本次新增约定）**：
  - SAJED：RSGT `>19hrs`(19) + DPW `NIL`(本周无船) → 取**有船码头 min=19**，忽略无船码头的 NIL（无船≠等泊0）。
  - LYBEN：整港 `NIL`(本周无船) → **0**（无拥堵报告=正常）。
- 12 港变更：CNNGB 48→36、CNSHK 24→72、CNTAO 48→72、YEADE 24→96、SAJED 6→19、INMUN 72→24、MYPKN 120→16、MYPKW 48→60、LYMRA 24→12、THBKK 48→15、SADMM 0→2、LYBEN 24→0。
- JOAQA(148>240hrs) 不在 ports.csv，未新增（守"仅更新已有港"）。
- 流程：fetch+checkout 对齐(origin/main 7442a46) → csv 重写第6列(保留 BOM+CRLF) → `git diff` 仅12行/只动第6列 → `audit_data_loss.py` PASS(退出码0) → commit+push → `git show origin/main` 验证。
- 备份：/tmp/ports.csv.bak（Git Bash /tmp）。
