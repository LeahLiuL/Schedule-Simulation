# 2026-08-06

## distances.csv 新增 OMDQM-CNTAO = 5619 nm
- 用户指示把待确认项 OMDQM(AL DUQM, 19.67/57.70) → CNTAO(QINGDAO, 36.07/120.38) 固化为实测距离。
- 追加行 `OMDQM,CNTAO,5619,manual`（CRLF、无 BOM，追加前断言 OMDQM 未存在）。此前 distances.csv 中 OMDQM 出现次数为 0。
- 合理性校验：大圆直线 3411nm（穿印度次大陆陆地）< 5619；L3 海域路由估算 5768nm（偏差 -2.6%）；横向对比 AEKLF→CNTAO=5810、AEJEA→CNTAO=5995，Duqm 不进波斯湾故更短，一致。
- source 用 `manual`：HTML `srLoadRouter()` 只剔除 `src==='auto'`，故 manual/空均会进 L2 图；manual 在 Data Manager 中有独立徽标。
- 流程：git fetch → ahead=0/behind=0 → 改 → `audit_data_loss.py` PASS(0 丢失) → commit `5ff52a5` → push → `git show origin/main` + raw.githubusercontent 双重验证已上线。
- 引擎复查：`R.distance('OMDQM','CNTAO')` 双向返回 `(5619.0, 'L1')`，命中实测直连。

## Simulator「Export Excel」改为 ExcelJS 专业排版（commit e409d56）
- 需求：下载的表格要专业、且格式与网页表格一致。原 `exportExcel()` 用 SheetJS `aoa_to_sheet`，纯数据无样式（SheetJS 社区版不支持单元格样式）。
- 改用 **ExcelJS 4.4.0**（HTML 第13行早已引入，此前只有 `exportVesselExcel()` 在用）。
- 新增 `XL_PAL` 调色板，色值直接取自表格 CSS：表头 `#1A3A5C`、斑马 `#F7FAFD`、修改行 `#FFF9E6`、网格 `#D0D9E4`；列色沿用 `.col-*`：PORT `#1A3A5C` 粗体、ETA/ETB `#1A6B3C`、ETD `#8B1A1A`、dist `#555`、fuel `#8B5E00`、voy `#17A2B8`、remark `#6C757D`；Delay 还原 `.status-badge` 三色块。
- `xlColSpec(col)` 按 `COL_DEFS.key` 返回 {w, align, color, bold, numFmt, date, badge, total}，导出自动跟随「☰ Columns」隐藏列设置（已测隐藏8列场景，SUM 公式列引用与筛选范围自动跟随）。
- **踩坑（重要）**：ExcelJS 把 Date 按 UTC 序列化，GMT+8 下所有 ETA/ETB/ETD 会整体少 8 小时。修复用 `xlDate(d)= new Date(d.getTime() - d.getTimezoneOffset()*60000)` 预偏移。**以后凡用 ExcelJS 写日期都要套 xlDate。**
- 专业增强：3 行抬头（标题/航线/参数）、冻结首列+表头(`xSplit:1,ySplit:4`)、自动筛选、TOTAL 行 SUM 公式、日期/数字用真类型+numFmt（可排序计算）、列宽按内容自适应(上限42)、横向打印 fitToWidth + `printTitlesRow`。
- Summary 分节重排（Voyage Info / Timing / Distance & Fuel / Meta），新增总航程天数、总海上天数；section 行用 `label==='#'` 标记并重置斑马计数。
- 验证方法（可复用）：`archive/export_excel/verify_export_excel.cjs` —— 从 HTML 正则切片提取**真实**导出源码，在 node vm 里 mock DOM 后用真 exceljs 跑出 xlsx，再用 openpyxl 读回逐项断言 fill/font/numFmt/merge/formula/freeze。注意切片前要 `.replace(/\r\n/g,'\n')`（文件是 CRLF）。

## Simulator「Export Excel」新增实时公式联动（2026-08-06）
- 需求：下载的 Excel 要带计算公式，改一个时间后面自动重算（"该时间的话后面的时间也会更新"）。
- Export Excel 按钮旁新增「实时公式」勾选框（默认勾选）。勾选时 ETA/ETB/ETD/Run/Fuel 写成 Excel 公式而非静态值。
- 公式逻辑（与网页 computeSchedule 1:1）：ETA_i=ROUNDUP((ETD_{i-1}+dist/speed/24)*24,0)/24（=ceilToHour），ETB=ETA+(manIn+wait)/24，ETD=ETB+stay/24；Run=dist/speed；Fuel=dist/speed*lsfo/24（lsfo 常数内联）。首港 ETA 为可编辑静态种子。N() 包裹 manIn/wait/stay 防空文本；IFERROR 防 speed=0 除零。
- **强制携带链路列** CHAIN_KEYS=[eta,etb,etd,dist,speed,manIn,wait,stayHrs,runHrs]：即使网页隐藏也导出，保证公式不破链；其余列仍跟随「☰ Columns」隐藏设置。Summary 首/末 ETA 与合计用 `'Voyage Schedule'!...` 引用主表，也变实时。
- 取消勾选 → 退回原静态快照（0 公式，原 e409d56 行为不变）。
- 验证：`verify_export_formula.cjs`（FORMULA 环境变量切静态/公式，node 提取真实源码跑出 xlsx，确认公式模式 29 个公式单元格、静态模式 0 个）+ `evaluate_formulas.py`（自写最小公式求值器直接 eval xlsx 内真实公式：Total Run=768.42h、Dist=11142.1nm、Fuel=880.48mt、SeaDays=32.02 自洽；首港 ETA+5h → 下游全部 +5h 联动证明）。

## Data Manager「Paste & Parse / Upload File」TCD 识别重写（未完成上线）
- 方向（用户拍板）：直接增强网页内 Vessels 的 Paste&Parse + Upload 功能，解析 TCD 文本（pdf.js/mammoth.js 抽取后塞进 textarea → `parseVesselTextClient` → `displayParseResult` → `saveParseVessel`）。本地 `tcd_intake.py` 仅作备胎，未 commit。
- 重写 `parseVesselTextClient` + `buildFuelTiers`：覆盖真实 TCD 各种怪格式（一行多字段如 `GT:16925/NT:9478LOA:156.3m`、无空格粘连、`Max 13 knots on about 20 mtons IFO 180`、HSFO 牌号、`Main engine/Auxiliary engine: LSFO 180/380 LSMGO` 章节标题自带数值、`About 1.2 mt IFO 180 CST (without reefer...)` 等）。粘度表(180/380…)过滤避免把牌号当油耗；年份取 `(?:19|20)\d{2}`。
- 转置 `cul_ship_particular.csv` 读写：PARTICULAR_DATA 是**对象数组**（每元素一行，key=表头单元格），`parseCSV` 对重复表头加 " (2)"。重写 `particularToCSV`/`appendParticularColumn`/`particularHeaderKeys`/`PARTICULAR_LABEL_TO_KEY`（全大写标签映射）。
- 测试三件套（node 沙箱跑**真实**源码，非 mock 解析器）：`archive/export_excel/test_parse_harness.cjs`(54/54)、`test_particular_csv.cjs`(CSV 往返0丢失+追加新船列正确)、`test_save_integration.cjs`(真实 parse→display→saveParseVessel 无 token 路径，断言生成的 vessels.csv + cul_ship_particular.csv 正确；25/25)。
- **关键 bug（本轮修）**：`saveParseVessel` 的 pMap 把 `reefer_plugs` 值错写成 DOM 后缀 `'reefer'`（应为 canonical key `'reefer_plugs'`），导致 `particular['reefer_plugs']` 未被写入、REEFER PLUGS 列留空。修复：模态框 input id 由 `pv_reefer` → `pv_reefer_plugs`，`displayParseResult` fieldMap 与 `saveParseVessel` pMap 统一用 `reefer_plugs`。**教训：pMap 的 value 必须是 canonical key（=pv_ 后缀去掉 `pv_`），DOM 元素 id 一律用 `pv_<canonicalkey>` 保持同一套。**
- 状态：**未推送**。用户原话"你得保证识别的TCD的内容是准确的，你得先测试好再上线，你弄好之后我可以给你几个PDF, Word文档进行一些测试再上线" → 等用户给真实 PDF/Word TCD 样本验收后再 commit+push。本地 shipping_schedule.html 改动 537+/149-，基于 main=4eb3099（比 origin/main cb7dc69 落后 1 个 SFTP 离港数据 commit，仅动 bi_vessel_departure.csv，对 shipping_schedule.html 无冲突）。
